import { AccidentStatisticDto } from './accident-statistic-dto';

describe('AccidentStatisticDto', () => {
  it('should create an instance', () => {
    expect(new AccidentStatisticDto()).toBeTruthy();
  });
});
